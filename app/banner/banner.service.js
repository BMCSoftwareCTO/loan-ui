(function () {
    'use strict';

    angular.module('myApp.banner.service', [])
        .factory('bannerService', ['$rootScope', '$resource', '$state',
            function ($rootScope, $resource, $state) {

            var bannerService={

            };
        	return bannerService;
        }]);
}());

